name: flutter_sqflite_example
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^1.1.7+1
  path_provider: ^1.4.0
  mobx: ^1.2.1+4
  flutter_mobx: ^1.1.0+2
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: 1.10.4
  mobx_codegen: ^1.1.1+3

flutter:
  uses-material-design: true


